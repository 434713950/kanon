spring:
  application:
    name: kanon-message-service
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: kanon-config-server
        enabled: true
      profile: ${spring.profiles.active}
      label: ${spring.profiles.active}

aliyun:
  access_key_id: LTAIb3SUxr20iO97
  access_key_secret: tb88N10tTHoRNTzLHzKvTpXVahK5eI

dingtalk:
  #钉钉企业id
  crop_id: ding5c225a9412eac936
  #钉钉企业密码
  crop_secret: gE4BpPK3jVCByg1xNcDRjD-Ygl9AqxQtxcAPKQK-D4A--f2S7pnfmPPGhxZ-nCTM

---
spring:
  profiles: dev
eureka:
  server:
    user:
      name: air
      password: clannad
    ip: 127.0.0.1
    host: 1025
    name: eureka
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    serviceUrl:
      defaultZone: http://${eureka.server.user.name}:${eureka.server.user.password}@${eureka.server.ip}:${eureka.server.host}/${eureka.server.name}
    registry-fetch-interval-seconds: 10

---
spring:
  profiles: prd

eureka:
  server:
    user:
      name: air
      password: clannad
    ip: 127.0.0.1
    host: 1025
    name: eureka
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.server.user.name}:${eureka.server.user.password}@${eureka.server.ip}:${eureka.server.host}/${eureka.server.name}